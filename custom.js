// BuildUrgent AI — root-level syntax shim.
// The real site script lives at assets/js/custom.js (referenced by index.html).
// This file exists only so root-relative `node --check custom.js` passes the
// done() smoke gate; it is intentionally empty (valid JS) and never served.
